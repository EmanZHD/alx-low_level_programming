#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: character to check
 * Return: if c is lowercase returns 1, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
