#include "main.h"

/**
 * _abs - faunction that cumputs the absolute value of a num
 * @a: input
 * Return: positive value of ab
 */
int _abs(int a)
{
	if (a < 0)
	{
		a = -a;
		return (a);
	}
	else
		return (a);
}
