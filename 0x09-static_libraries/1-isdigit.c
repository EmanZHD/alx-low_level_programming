#include "main.h"
/**
 * _isdigit - function that verifies if it's a digit or not
 * @c: input
 * Return: return 1 if c is a digit && 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
